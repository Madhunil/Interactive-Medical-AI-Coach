# Core Streamlit (Latest stable)
streamlit>=1.31

# AWS SDK (Latest)
boto3>=1.34
botocore>=1.34

# Environment management
python-dotenv>=1.0

# Audio processing (Latest compatible)
SpeechRecognition>=3.10
pyaudio>=0.2
soundfile>=0.12
pydub>=0.25

# Image processing (Latest stable - fixes DLL issues)
Pillow>=10.2

# Logging (Latest)
loguru>=0.7

# Scientific computing (Latest)
numpy>=1.26
pandas>=2.2

# HTTP requests (Latest)
requests>=2.31

# Wave processing (built-in)
wave